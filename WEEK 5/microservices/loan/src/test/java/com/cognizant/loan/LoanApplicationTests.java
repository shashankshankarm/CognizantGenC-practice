package com.cognizant.loan;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

// This test class verifies that the application context loads without any issues.
@SpringBootTest
class LoanApplicationTests {

    @Test
    void shouldInitializeApplicationContext() {
        // No implementation needed; test will fail if context does not load
    }
}
